# npm packages — restored with `npm install`
node_modules/

# Vite build output (re-generated by `wails build`)
# .gitkeep stays committed so the //go:embed all:frontend/dist directive still resolves on a fresh clone
frontend/dist/*
!frontend/dist/.gitkeep

# Wails build artifacts
build/bin/

# Compiled app binary
/pengo-browser

# Binaries
*.exe
*.dll
*.so
*.dylib

# Compiled Go binaries (Linux/macOS executables have no extension)
/cmd/client/client
/cmd/server/server
/cmd/dns/dns

# Same binaries when built from the repo root (go build ./cmd/...)
/client
/server
/dns

# Test binary and coverage
*.test
*.out

# Build output
/bin/
/dist/

# Go workspace file
go.work
go.work.sum

# Env / secrets
.env

# DNS registry — real entries stay local, commit the template only
registry.json

# md
*.md
!README.md
